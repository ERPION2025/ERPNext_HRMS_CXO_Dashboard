[project]
name = "hr_analytics"
authors = [
    { name = "ERPion Technologies LLP", email = "hello@erpion.in" }
]
description = "Native ERPNext/HRMS dashboards: Recruitment, Onboarding, Probation, Leave, Appraisal and Executive reporting, with a UI-driven widget builder and workflow-stage visuals."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
